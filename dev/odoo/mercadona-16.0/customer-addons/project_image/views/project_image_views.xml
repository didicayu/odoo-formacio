<odoo>
    <record id="image_on_form" model="ir.ui.view">
        <field name="name">project_image.form</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='description']" position="after">
                <field name="show_image" invisible="1"/>
<!--                attrs="{'readonly': [('id', '!=', False)]}-->
                <field name="image_1920" string="Project image" widget="image"  attrs="{'invisible': [('show_image', '=', False)]}" groups="base.group_user"/>
                <field name="image_1920" string="Project image" widget="image"  attrs="{'invisible': [('show_image', '=', False)]}" groups="project_image.group_project_image"/>
            </xpath>
        </field>
    </record>

    <record id="image_on_kanban" model="ir.ui.view">
        <field name="name">project_image.kanban</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.view_project_kanban"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@class='o_kanban_primary_left']" position="after">
                <div>
                    <field name="image_256" string="Project image" widget="image"  attrs="{'invisible': [('show_image', '=', False)]}"/>
                    <field name="show_image" invisible="1"/>
                </div>
            </xpath>
        </field>
    </record>
</odoo>