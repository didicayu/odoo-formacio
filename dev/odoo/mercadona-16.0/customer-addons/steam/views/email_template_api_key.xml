<?xml version="1.0"?>
<odoo>
    <data noupdate="1">
        <!-- Email Template -->
        <record id="email_template_api_key" model="mail.template">
            <field name="name">API Key Notification</field>
            <field name="model_id" ref="base.model_res_users"/>
            <field name="subject">Your API Key</field>
            <field name="email_from">${object.env.user.company_id.email or 'noreply@example.com'}</field>
            <field name="email_to">${object.email}</field>
            <field name="auto_delete" eval="True"/>
            <field name="lang">${object.lang}</field>
            <field name="body_html"><![CDATA[
                <p>Dear ${object.name},</p>
                <p>Your API key has been generated successfully. Here is your API key:</p>
                <p><strong>${object.api_key}</strong></p>
                <p>Please keep it secure and do not share it with anyone.</p>
                <p>Best regards,<br/>
                ${object.env.user.company_id.name}</p>
            ]]></field>
        </record>
    </data>
</odoo>
